package singleton;

/**
 * 单例模式:饿汉式
 * @author DELL
 *
 */
public class Stu {
	private Stu() {
		
	}   
	//将构造方法私有化,就不能再new该类的对象了
	private static Stu stu = new Stu();
	//静态区域只能执行一次
	public static Stu getInstance() {
		return stu;
	//让类直接调用静态方法返回在加载时生成的唯一的对象
	}
}
